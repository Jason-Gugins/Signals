# Techstack probe fixtures (2026-08-23)

Live Playwright Chromium, headless. `domcontentloaded` + 2500ms. Query stripped. Cap 80 URLs.

clutch.co is **not** here (HTTP 403 Cloudflare challenge — no fake 200 fixture).

## usercentrics.com — HTTP 200

Fixture: `network_usercentrics.com.json` (80 reqs)

Third-party / adjacent hosts actually present:

| host | note |
|---|---|
| `privacy-proxy-server.usercentrics.eu` | first-party CMP proxy (other eTLD) |
| `sst.usercentrics.com` | first-party SST |

No GTM/OneTrust/HubSpot hosts in this HAR (first 80 were almost all first-party). GTM was seen earlier in **HTML** `script src` only.

## levitate.ai — HTTP 200

Fixture: `network_levitate.ai.json` (71 reqs)

| host | likely |
|---|---|
| `cdn.prod.website-files.com` | Webflow |
| `hubspotonwebflow.com` | HubSpot |
| `cdn.cookielaw.org` | OneTrust |
| `cdn.jsdelivr.net` | jsDelivr |
| `d3e54v103j8qbb.cloudfront.net` | Webflow CloudFront |
| `ajax.googleapis.com` / `fonts.googleapis.com` / `fonts.gstatic.com` | Google infra |
| `marketing.levitate.ai` | first-party |

## darktrace.com — HTTP 200

Fixture: `network_darktrace.com.json` (80 reqs)

| host | likely |
|---|---|
| `cdn.prod.website-files.com` | Webflow |
| `hubspotonwebflow.com` | HubSpot |
| `www.googletagmanager.com` | GTM |
| `cdn.cookielaw.org` | OneTrust |
| `geolocation.onetrust.com` | OneTrust |
| `bat.bing.com` | Bing UET |
| `player.vimeo.com` | Vimeo |
| `cdn.intellimize.co` | Intellimize |
| `www.googleadservices.com` / `googleads.g.doubleclick.net` / `pagead2.googlesyndication.com` | Google Ads |
| `uk01.z.antigena.com` | Darktrace product |
| `ob.seaskypro.com` / `obs.seaskypro.com` | third-party |

## Do not add to YAML from this file unless the host is in a fixture above

Absent here: Segment, Intercom, Marketo, CookieYes, `challenges.cloudflare.com` (Clutch only, not frozen).
